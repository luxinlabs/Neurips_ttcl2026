From 6f14fc0edfb4a04393d718898b17ef35f261c78d Mon Sep 17 00:00:00 2001
From: Author <author@example.com>
Date: Wed, 26 Aug 2026 03:48:29 +0000
Subject: [PATCH] Fix stochastic channel routing + capture selection retention
 metric

- NoisyMemoryStore: route retrieval through the approximate index over the
  staleness-persisted set, so the stochastic channel actually determines which
  neighbours are returned (previously the approx index was measured for recall@k
  but never queried, leaving the stochastic channel causally inert). recall@k is
  now ground-truthed against the same persisted set the agent sees.
- matched_replay: optional measure_retention (default off) threads an unbounded
  per-instance oracle run into each store so retrieve() reports retention_rate
  for the selection channel (was always None). Costs one extra rollout/instance.
- tests: add regression guard asserting retrieval delegates to the approximate
  index (fails on the old exact-shadow path). Full suite 45 passing.
---
 driftbench/index/noisy_memory.py    | 59 ++++++++++++++++++++++++-----
 driftbench/replay/matched_replay.py | 20 ++++++++++
 tests/test_noisy_memory.py          | 22 +++++++++++
 3 files changed, 91 insertions(+), 10 deletions(-)

diff --git a/driftbench/index/noisy_memory.py b/driftbench/index/noisy_memory.py
index 216814d..dd48fc8 100644
--- a/driftbench/index/noisy_memory.py
+++ b/driftbench/index/noisy_memory.py
@@ -2,6 +2,22 @@
 main reusable artifact of the harness: one object another group can point at
 their own agent stack to get a drift-under-infra readout, per
 [INSIGHT: intro_contributions] (the harness as its own headline deliverable).
+
+Retrieval routing (fixed): a write first lands in a staleness buffer and only
+becomes searchable when the rebuild cadence flushes it into the *approximate*
+index. Retrieval then runs approximate search over that persisted set and is
+gated by the selection budget. This is what puts all three channels on the
+actual retrieval path at once:
+
+  * systematic (staleness): a pending write is invisible until the next rebuild;
+  * stochastic (approximate search): the persisted set is searched approximately,
+    so the stochastic level actually determines which neighbours come back
+    (previously the approximate index was measured but never queried, so the
+    stochastic channel could not causally affect behaviour);
+  * selection (budget): eviction gates which persisted ids are visible at all.
+
+ApproxIndex keeps an internal exact reference over exactly the vectors it holds,
+so recall@k is computed against the same persisted set the agent actually sees.
 """
 from __future__ import annotations
 
@@ -11,7 +27,7 @@ import numpy as np
 
 from driftbench.index.approx_index import ApproxIndex, ApproxIndexConfig
 from driftbench.index.eviction import BudgetedMemory, EvictionConfig
-from driftbench.index.shadow_index import ShadowIndex, ShadowIndexConfig
+from driftbench.index.shadow_index import ShadowIndexConfig
 
 
 @dataclass
@@ -29,7 +45,7 @@ class RetrievalReport:
 class NoisyMemoryStore:
     """A single agent-memory store wired to expose stochastic, systematic,
     and selection noise as independently controllable, independently
-    measurable channels."""
+    measurable channels — all three on the actual retrieval path."""
 
     def __init__(
         self,
@@ -40,8 +56,16 @@ class NoisyMemoryStore:
         oracle: "NoisyMemoryStore | None" = None,
     ):
         self.dim = dim
+        # Persisted, searchable index (approximate). Also holds an internal
+        # exact reference over the same vectors, so recall@k is ground-truthed
+        # against exactly what is searchable.
         self._approx = ApproxIndex(dim, stochastic)
-        self._shadow = ShadowIndex(dim, systematic)
+        # Systematic channel: a write is buffered here and only flushed into
+        # _approx every `rebuild_cadence` writes (K=1 => always fresh).
+        self._rebuild_cadence = max(1, systematic.rebuild_cadence)
+        self._pending: list[tuple[int, np.ndarray]] = []
+        self._writes_since_rebuild = 0
+        # Selection channel: budgeted visibility over all writes.
         self._budget = BudgetedMemory(selection or EvictionConfig())
         self._oracle = oracle  # unbounded-memory reference run, for retention_rate
         self._next_id = 0
@@ -51,17 +75,32 @@ class NoisyMemoryStore:
         item_id = self._next_id
         self._next_id += 1
         vector = np.ascontiguousarray(vector, dtype=np.float32)
-        self._approx.add(vector.reshape(1, -1), [item_id])
-        self._shadow.write(vector, item_id)
+        # Buffer first (staleness); persist into the searchable index on cadence.
+        self._pending.append((item_id, vector))
+        self._writes_since_rebuild += 1
+        if self._writes_since_rebuild >= self._rebuild_cadence:
+            self._rebuild()
+        # The budget tracks every write immediately — eviction is a visibility
+        # gate independent of index freshness.
         self._budget.add(item_id, vector, importance=importance)
         self._payloads[item_id] = payload
         return item_id
 
+    def _rebuild(self) -> None:
+        if not self._pending:
+            self._writes_since_rebuild = 0
+            return
+        vecs = np.stack([v for _, v in self._pending])
+        ids = [i for i, _ in self._pending]
+        self._approx.add(vecs, ids)
+        self._pending.clear()
+        self._writes_since_rebuild = 0
+
     def retrieve(self, query: np.ndarray, k: int) -> RetrievalReport:
-        # Selection (budget) gates what's visible at all; among the budgeted
-        # ids, staleness/recall are read off the approx/shadow instrumentation.
+        # Approximate search over the persisted set is the retrieval path;
+        # the budget then gates which of those ids are visible.
         visible_ids = self._budget.ids()
-        candidate_ids = self._shadow.search(query, k)
+        candidate_ids = self._approx.search(query, k)
         retrieved_ids = [i for i in candidate_ids if i in visible_ids][:k]
         for i in retrieved_ids:
             self._budget.touch(i)
@@ -73,7 +112,7 @@ class NoisyMemoryStore:
         return RetrievalReport(
             retrieved_ids=retrieved_ids,
             retrieved_payloads=[self._payloads.get(i, "") for i in retrieved_ids],
-            staleness=self._shadow.staleness_at_query(),
+            staleness=len(self._pending),  # systematic: writes not yet searchable
             recall_at_k=self._approx.recall_at_k(query, k),
             retention_rate=retention,
         )
@@ -81,4 +120,4 @@ class NoisyMemoryStore:
     def flush(self) -> None:
         """Force any pending writes into the searchable index — call at
         episode boundaries per the testbed's protocol, or for the oracle run."""
-        self._shadow.force_rebuild()
+        self._rebuild()
diff --git a/driftbench/replay/matched_replay.py b/driftbench/replay/matched_replay.py
index 07f4032..fd1e2da 100644
--- a/driftbench/replay/matched_replay.py
+++ b/driftbench/replay/matched_replay.py
@@ -16,6 +16,7 @@ from typing import Callable, Protocol
 
 from driftbench.config import ConditionSpec, ExperimentGrid
 from driftbench.eval.seqmem_protocol import SeqMemTrace
+from driftbench.index.eviction import EvictionConfig
 from driftbench.index.noisy_memory import NoisyMemoryStore
 
 
@@ -45,6 +46,7 @@ def run_matched_replay(
     dim: int,
     include_selection: bool = True,
     n_samples_per_cell: int = 1,
+    measure_retention: bool = False,
 ) -> list[ReplayResult]:
     """For every task instance, replay it once per grid condition (9 factorial
     cells, plus Selection's one-at-a-time conditions if `include_selection`).
@@ -71,6 +73,23 @@ def run_matched_replay(
         conditions += list(grid.selection_conditions)
 
     for instance in instances:
+        # Selection channel's retention metric compares a budgeted memory to an
+        # unbounded-memory ("oracle") run of the SAME task instance. When
+        # requested, run that oracle once per instance (baseline infra, no
+        # eviction) and thread it into every condition's store so retrieve()
+        # can report retention_rate. NOTE: this compares against the oracle's
+        # final contents; per-query lockstep time-alignment is a refinement
+        # left as a TODO. Off by default: it costs one extra rollout/instance.
+        oracle_store: NoisyMemoryStore | None = None
+        if measure_retention and include_selection:
+            oracle_store = NoisyMemoryStore(
+                dim=dim,
+                stochastic=grid.baseline.stochastic,
+                systematic=grid.baseline.systematic,
+                selection=EvictionConfig(budget=None),  # unbounded
+            )
+            episode_runner(instance, grid.baseline, oracle_store, 0)
+
         for condition in conditions:
             for replicate in range(n_samples_per_cell):
                 store = NoisyMemoryStore(
@@ -78,6 +97,7 @@ def run_matched_replay(
                     stochastic=condition.stochastic,
                     systematic=condition.systematic,
                     selection=condition.selection,
+                    oracle=oracle_store,
                 )
                 trace = episode_runner(instance, condition, store, replicate)
                 results.append(
diff --git a/tests/test_noisy_memory.py b/tests/test_noisy_memory.py
index 59ae2b9..c5ffeb8 100644
--- a/tests/test_noisy_memory.py
+++ b/tests/test_noisy_memory.py
@@ -67,3 +67,25 @@ def test_flush_makes_pending_writes_searchable():
 
     store.flush()
     assert store.retrieve(vec, k=1).retrieved_ids == [0]
+
+
+def test_retrieval_routes_through_the_approximate_index():
+    """Regression guard for the stochastic channel being on the retrieval path.
+
+    Under an approximate (HNSW) index, the retrieved ids must be exactly what
+    the approximate index returns (budget unbounded here). On the previous
+    implementation retrieval came from an exact shadow index, so the stochastic
+    level could not affect what was retrieved; that version fails this test.
+    """
+    dim = 8
+    store = NoisyMemoryStore(
+        dim=dim,
+        stochastic=ApproxIndexConfig(kind="hnsw", hnsw_m=8, ef_search=8),
+        systematic=ShadowIndexConfig(rebuild_cadence=1),
+    )
+    rng = np.random.default_rng(0)
+    for _ in range(20):
+        store.write(rng.standard_normal(dim).astype(np.float32))
+    q = rng.standard_normal(dim).astype(np.float32)
+    report = store.retrieve(q, k=3)
+    assert report.retrieved_ids == store._approx.search(q, k=3)[:3]
-- 
2.43.0

